export const defaultStarterSettings = {
  enabled: false,

  // Default values loaded on first run
  pairs: [
    {
      key: '$$epoch_number',
      value: 'DefaultStarterSettings',  
    },
  ]
};

